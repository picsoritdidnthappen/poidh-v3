# Copy to `.env` and fill values (do not commit `.env`).

# Deploy script inputs
DEPLOYER_PK=
POIDH_TREASURY=
POIDH_START_CLAIM_INDEX=1

# Optional (recommended): Safe multisig to own the claim NFT
POIDH_MULTISIG=

# Optional: claim NFT metadata
POIDH_NFT_NAME="poidh claims v3"
POIDH_NFT_SYMBOL="POIDH3"

# Base / Base Sepolia helpers (see `foundry.toml` rpc_endpoints + etherscan config)
BASE_SEPOLIA_RPC_URL=
BASE_RPC_URL=
BASESCAN_API_KEY=

# Optional: fork test config
FORK_URL=
FORK_BLOCK=
